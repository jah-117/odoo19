<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        education_core — Record Rules
        Source: Educational_ERP_Comprehensive_Project_Submission.docx
        Multi-company isolation rules for all core models.
        Additional row-level rules (student own-record, teacher own-class)
        are added in Sprint 2 when enrollment and attendance are implemented.
    -->

    <!-- ── Multi-company: education.academic.year ─────────────────────── -->
    <record id="rule_academic_year_company" model="ir.rule">
        <field name="name">Academic Year: company isolation</field>
        <field name="model_id" ref="model_education_academic_year"/>
        <field name="domain_force">
            ['|', ('company_id', '=', False),
                  ('company_id', 'in', company_ids)]
        </field>
        <field name="perm_read">1</field>
        <field name="perm_write">1</field>
        <field name="perm_create">1</field>
        <field name="perm_unlink">1</field>
    </record>

    <!-- ── Multi-company: education.department ───────────────────────── -->
    <record id="rule_department_company" model="ir.rule">
        <field name="name">Department: company isolation</field>
        <field name="model_id" ref="model_education_department"/>
        <field name="domain_force">
            ['|', ('company_id', '=', False),
                  ('company_id', 'in', company_ids)]
        </field>
        <field name="perm_read">1</field>
        <field name="perm_write">1</field>
        <field name="perm_create">1</field>
        <field name="perm_unlink">1</field>
    </record>

    <!-- ── Multi-company: education.program ──────────────────────────── -->
    <record id="rule_program_company" model="ir.rule">
        <field name="name">Program: company isolation</field>
        <field name="model_id" ref="model_education_program"/>
        <field name="domain_force">
            ['|', ('company_id', '=', False),
                  ('company_id', 'in', company_ids)]
        </field>
        <field name="perm_read">1</field>
        <field name="perm_write">1</field>
        <field name="perm_create">1</field>
        <field name="perm_unlink">1</field>
    </record>

    <!-- ── Multi-company: education.class ────────────────────────────── -->
    <record id="rule_class_company" model="ir.rule">
        <field name="name">Class: company isolation</field>
        <field name="model_id" ref="model_education_class"/>
        <field name="domain_force">
            ['|', ('company_id', '=', False),
                  ('company_id', 'in', company_ids)]
        </field>
        <field name="perm_read">1</field>
        <field name="perm_write">1</field>
        <field name="perm_create">1</field>
        <field name="perm_unlink">1</field>
    </record>

    <!-- ── Multi-company: education.timetable ────────────────────────── -->
    <record id="rule_timetable_company" model="ir.rule">
        <field name="name">Timetable: company isolation</field>
        <field name="model_id" ref="model_education_timetable"/>
        <field name="domain_force">
            ['|', ('class_id.company_id', '=', False),
                  ('class_id.company_id', 'in', company_ids)]
        </field>
        <field name="perm_read">1</field>
        <field name="perm_write">1</field>
        <field name="perm_create">1</field>
        <field name="perm_unlink">1</field>
    </record>

    <!-- ── Multi-company: education.application ──────────────────────── -->
    <record id="rule_application_company" model="ir.rule">
        <field name="name">Application: company isolation</field>
        <field name="model_id" ref="model_education_application"/>
        <field name="domain_force">
            ['|', ('company_id', '=', False),
                  ('company_id', 'in', company_ids)]
        </field>
        <field name="perm_read">1</field>
        <field name="perm_write">1</field>
        <field name="perm_create">1</field>
        <field name="perm_unlink">1</field>
    </record>

    <!-- ── Portal: student sees own application only ─────────────────── -->
    <record id="rule_application_portal_own" model="ir.rule">
        <field name="name">Application: portal user sees own only</field>
        <field name="model_id" ref="model_education_application"/>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="domain_force">
            [('email', '=', user.email)]
        </field>
        <field name="perm_read">1</field>
        <field name="perm_write">0</field>
        <field name="perm_create">0</field>
        <field name="perm_unlink">0</field>
    </record>

    <!-- ── Multi-company: education.enrollment ───────────────────────── -->
    <record id="rule_enrollment_company" model="ir.rule">
        <field name="name">Enrollment: company isolation</field>
        <field name="model_id" ref="model_education_enrollment"/>
        <field name="domain_force">
            ['|', ('company_id', '=', False),
                  ('company_id', 'in', company_ids)]
        </field>
        <field name="perm_read">1</field>
        <field name="perm_write">1</field>
        <field name="perm_create">1</field>
        <field name="perm_unlink">1</field>
    </record>

    <!-- ── Portal: student sees own enrollment only ───────────────────── -->
    <record id="rule_enrollment_portal_own" model="ir.rule">
        <field name="name">Enrollment: portal user sees own only</field>
        <field name="model_id" ref="model_education_enrollment"/>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="domain_force">
            [('student_partner_id', '=', user.partner_id.id)]
        </field>
        <field name="perm_read">1</field>
        <field name="perm_write">0</field>
        <field name="perm_create">0</field>
        <field name="perm_unlink">0</field>
    </record>

    <!-- ── Multi-company: education.faculty ─────────────────────────── -->
    <record id="rule_faculty_company" model="ir.rule">
        <field name="name">Faculty: company isolation</field>
        <field name="model_id" ref="model_education_faculty"/>
        <field name="domain_force">[('company_id', 'in', company_ids)]</field>
        <field name="groups" eval="[(4, ref('education_security.group_education_admin')),
                                    (4, ref('education_security.group_education_teacher'))]"/>
    </record>

    <!-- ── Portal: student sees own documents only ────────────────────── -->
    <record id="rule_document_portal_own" model="ir.rule">
        <field name="name">Document: portal user sees own only</field>
        <field name="model_id" ref="model_education_document"/>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="domain_force">
            [('enrollment_id.student_partner_id', '=', user.partner_id.id)]
        </field>
        <field name="perm_read">1</field>
        <field name="perm_write">1</field>
        <field name="perm_create">1</field>
        <field name="perm_unlink">0</field>
    </record>

</odoo>
