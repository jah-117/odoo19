<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="action_hotel_guests" model="ir.actions.act_window">
        <field name="name">Hotel Guests</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="context">{'default_is_hotel_guest':True}</field>
        <field name="help" type="html">
             <p class="o_view_nocontent_smiling_face">
                Create Guests
            </p>
        </field>
    </record>

    <record id="view_partner_form_inherit_hotel_guest" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.hotel.guest</field>
        <field name="model">res.partner</field>
        <field name="type">form</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='category_id']" position="after">
                <field name="is_hotel_guest" string="Is Hotel Guest" invisible="not context.get('default_is_hotel_guest')"/>
            </xpath>
        </field>
    </record>
</odoo>